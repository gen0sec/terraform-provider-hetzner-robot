# Provide ONE of the following (both optional):

# (a) reference an EXISTING key already in your Robot key store, by NAME:
existing_ssh_key_name = "k8s-node-key"

# (b) OR upload a NEW key — paste your SSH PUBLIC key:
# ssh_public_key  = "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILExampleReplaceMe you@host"
# upload_key_name = "k8s-node-key"

# Optional overrides:
# product_id = "EX44"
# location   = "FSN1"
