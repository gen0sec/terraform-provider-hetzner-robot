---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "hetzner-robot_reset Resource - terraform-provider-hetzner-robot"
subcategory: ""
description: |-
  Triggers a Hetzner Robot server reset. Action-style resource: it performs the reset on create and is a no-op on destroy. Change triggers (e.g. to the boot profile) to force a new reset — typically chained after hetzner-robot_boot to apply an install/rescue profile.
---

# hetzner-robot_reset (Resource)

Triggers a Hetzner Robot server reset. Action-style resource: it performs the reset on create and is a no-op on destroy. Change `triggers` (e.g. to the boot profile) to force a new reset — typically chained after hetzner-robot_boot to apply an install/rescue profile.

## Example Usage

```terraform
# Set an install/rescue boot profile, then reset the server so it takes effect.
# `triggers` ties the reset to the boot config so changing the profile re-runs it.
resource "hetzner-robot_boot" "example" {
  server_number    = 123456
  active_profile   = "linux"
  operating_system = "Ubuntu 24.04 LTS minimal"
  language         = "en"
  authorized_keys  = ["aa:bb:cc:dd:ee:ff:00:11:22:33:44:55:66:77:88:99"]
}

resource "hetzner-robot_reset" "example" {
  server_number = hetzner-robot_boot.example.server_number
  reset_type    = "hw" # hw | sw | power | man

  triggers = {
    boot_profile = hetzner-robot_boot.example.active_profile
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `server_number` (Number) Number of the server to reset

### Optional

- `reset_type` (String) Reset type: hw (hardware), sw (Ctrl+Alt+Del), power (power cycle), or man (manual)
- `triggers` (Map of String) Arbitrary key/value pairs that force a new reset when changed (e.g. the boot profile).

### Read-Only

- `id` (String) The ID of this resource.
